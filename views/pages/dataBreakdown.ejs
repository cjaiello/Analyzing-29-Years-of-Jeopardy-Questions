<!DOCTYPE html>
<html>


  <head>
    <% include ../partials/header.ejs %>
    <title>Data Breakdown</title>
  </head>
  
  <body>
  
    <% include ../partials/nav.ejs %>
  <center>
    <div id="loadingBoxImage">
      <h2>Now loading...</h2>
    </div>
    <div id="directionsBox">
      <h2>Hover over a data point in the leftmost or middle chart to bring up a new chart to the right! Click any data point in the calendar chart to generate a Word Cloud below of all words from that year.</h2>
    </div>
  </center>
  <div id="wrapper">
    <div id="locationOfSVGs">
    </div>
    <div class="calendarBoxIndex">
    </div>
  </div>
  <center>
    <table>
      <tr>
        <td>
          <div id="locationOfWordCloud" align="center">
          </div>
        </td>
      </tr>
      <tr>
        <td>
          <div id="locationOfWordCloudLabel" align="center" class="white">
          </div>
        </td>
      </tr>
    </table>
  </center>
</body>

<script>
  d3.csv('/data/jeopardy_questions_and_answers_preprocessed_groupingvalues.csv', function(err, data) {
    if (err) console.log(err);

    // Aggregate data set
    aggregatedData = aggregateByRound(data);

    buildBarVis(aggregatedData, 85, 1, "Round");
  });

  // Initially show loading image
  showLoadingImage(true);
</script>

</html>
