<!DOCTYPE html>
<html>


  <head>
    <% include ../partials/header.ejs %>
  </head>
  
  <body>
  
    <% include ../partials/nav.ejs %>
  <center>
    <div id="loadingBoxImage">
      <h2>Now loading...</h2>
    </div>
    <div id="directionsBox">
      <h2>Type two words below to compare the frequencies of how often two words appear in all questions, answers, and categories (of the entire set of 216,931 data points)!<br>If you just want to search for one word, type one word into either of the boxes below and hit "Search."</h2>
    </div>
    <div id="inputDiv">
      <input type="text" id="wordToSearchFor1" placeholder="Type first word here" class="smallPadding"> <span class="white">versus</span> <input type="text" id="wordToSearchFor2" placeholder="Type second word here" class="smallPadding"><button type="submit" id="submitButton" onclick="findWordPairMatchesInDataSet()"
        class="smallPadding">Search</button>
    </div>
    <div id="wrapper">
      <div id="locationOfSVGsWordComparison">
      </div>
      <div id="locationOfJustOneCalendar">
      </div>
      <div class="calendars">
        <div class="calendarBoxWordComparisons1">
        </div>
        <div class="calendarBoxWordComparisons2" id="calendarBoxWordComparisons2">
        </div>
      </div>
    </div>
  </center>
  </div>
</body>

<script>
  // When people hit enter, have that also submit the answer
  $("#wordToSearchFor").keypress(function(e) {
    if (e.keyCode == 13) {
      event.preventDefault();
      // Check if input is valid:
      if (validateForm()) {
        findWordMatchesInDataSet();
      }
    }
  });
</script>

</html>
